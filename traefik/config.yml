http:
  services:
    jellyfin-svc:
      loadBalancer:
        servers:
        # should point to local address of jellyfin with http port (8096 by default)
          - url: {{ env "JELLYFIN_LOCAL_URL" }}
